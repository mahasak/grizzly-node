export enum ExperimentConstants {
    APPNAME_HEADER = 'EXPERIMENT-APPNAME',
    INSTANCEID_HEADER = 'EXPERIMENT-INSTANCE',
    USER_AGENT = 'USER-AGENT',
} 

export enum Operator {
    IN = <any>'IN',
    NOT_IN = <any>'NOT_IN',
}

export enum ParamType {
    STRING = 'string',
}